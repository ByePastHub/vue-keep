# Contributing

感谢你对 vue-keep 的贡献兴趣！

## 开发环境

```bash
# 克隆仓库
git clone https://github.com/ByePastHub/vue-keep.git
cd vue-keep

# 安装依赖
pnpm install

# 开发模式
pnpm dev

# 运行测试
pnpm test

# 类型检查
pnpm typecheck
```

## 提交规范

使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

- `feat:` 新功能
- `fix:` 修复
- `docs:` 文档
- `test:` 测试
- `chore:` 构建/工具

## Pull Request

1. Fork 仓库并创建分支
2. 确保测试通过：`pnpm test`
3. 确保类型检查通过：`pnpm typecheck`
4. 提交 PR 并描述变更内容
